{
  "kind": "build_request",
  "title": "Great Follower Wall background collage and per-name hover effects",
  "priority": "normal",
  "requirements": [],
  "constraints": [
    "Do not create new requirements for functionality that already exists in the project’s existing_features list (Great Follower Wall collage + per-name hover effects are already implemented features).",
    "Backend must remain a single Motoko actor in backend/main.mo; only create backend/migration.mo if an upgrade requires state migration.",
    "Do not edit files under frontend/immutablePaths (frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui)."
  ],
  "nonGoals": [
    "Adding new animation behaviors beyond the existing per-name hover effect (e.g., animations on name appearance) unless explicitly requested.",
    "Adding new backend endpoints or changing persistence schema for follower data unless needed to fulfill a new user request."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}